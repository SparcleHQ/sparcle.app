---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/ui/Hero.astro";
import PageSection from "../components/ui/PageSection.astro";
import CardGrid from "../components/ui/CardGrid.astro";
import Card from "../components/ui/Card.astro";
import CTAStrip from "../components/ui/CTAStrip.astro";

const tierRows = [
  ["Data plane",      "Your perimeter",          "Your perimeter"],
  ["AI inference",    "Your LLM (BYOK / BYO)",   "Sparcle-managed gateway (zero-retention)"],
  ["Hosting",         "You",                     "You"],
  ["SLA",             "Self-managed",            "Priority support"],
  ["Air-gap option",  "✓",                  "—"],
];
---
<BaseLayout
  title="Architecture — Sovereign Enterprise AI Runtime + ACL-Aware Data Plane | Sparcle"
  description="How Bolt's enterprise AI agent runtime and Aeira's governance-grade data plane fit together. Patent-pending priority engine, multi-layer security pipeline, Authority Policy SDK. Built for any enterprise; deployable for the most regulated. NDA briefs available."
  canonical="https://sparcle.app/architecture"
>
  <link slot="head" rel="stylesheet" href="/css/trust-callout.css" />
  <main class="page-architecture">
    <Hero
      eyebrow="Architecture"
      title="Built so your security team says yes."
      lede="Bolt and Aeira are designed for the enterprises that can't compromise on either. Below is the architectural posture at the level a buyer needs to evaluate fit. Detailed component briefs, security architecture decisions, and patent-protected implementation specifics are shared under NDA during pilot evaluation."
    >
      <a href="#" onclick="openContactModal('Architecture Review'); return false;" class="btn btn-primary btn-lg">
        Schedule Architecture Review
      </a>
      <a href="#" onclick="openContactModal('Architecture Brief Request'); return false;" class="btn btn-secondary btn-lg">
        Request Architecture Brief (NDA)
      </a>
    </Hero>

    <aside class="trust-callout" aria-label="Trust dossier">
      <div class="trust-callout__icon" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
        </svg>
      </div>
      <div class="trust-callout__body">
        <h3 class="trust-callout__title">Reading this for security review?</h3>
        <p class="trust-callout__lede">
          The complete trust dossier &mdash; sub-processor disclosure, controls evidence map, cryptographic-erasure whitepaper, standalone audit-chain verifier, incident-response framework, and vulnerability disclosure policy &mdash; lives at the Trust Center.
        </p>
        <a href="/trust" class="trust-callout__cta">
          Browse the Trust Center &nearr;
        </a>
      </div>
    </aside>

    <PageSection
      eyebrow="System overview"
      title="How Bolt and Aeira fit together inside your perimeter."
      lede="The customer perimeter contains every part of the runtime that touches data. Identity flows in from your existing IdP. Bolt is the agent runtime; Aeira is the data plane. Tools attach via open MCP. Only the LLM endpoint sits outside, and which mode it runs in is your choice."
      stripe="a"
    >
      <div class="arch-diagram" data-expandable tabindex="0" role="button" aria-label="Expand system overview diagram">
        <svg viewBox="0 0 900 480" class="arch-overview-svg" role="img" aria-labelledby="arch-overview-title arch-overview-desc">
          <title id="arch-overview-title">Bolt and Aeira system overview</title>
          <desc id="arch-overview-desc">The customer perimeter contains the Identity stack, the Bolt agent runtime, the Aeira data plane, and the MCP-connected tool surface. The LLM endpoint sits outside the perimeter and can be configured per the three deployment modes.</desc>

          <defs>
            <marker id="a-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
              <path class="a-arrow-head" d="M0,0 L10,5 L0,10 z" />
            </marker>
          </defs>

          <style>
            .a-perimeter { fill: none; stroke: var(--text-muted); stroke-width: 1.5; stroke-dasharray: 8 5; opacity: 0.55; }
            .a-perimeter-label { font: 700 11px var(--font-sans, system-ui); fill: var(--text-muted); letter-spacing: 0.12em; }
            .a-box { fill: var(--bg-surface-solid); stroke: var(--brand-primary); stroke-width: 1.5; }
            .a-box--soft { fill: var(--bg-surface-solid); stroke: var(--text-muted); stroke-width: 1; opacity: 0.92; }
            .a-box--ext { fill: var(--bg-surface-solid); stroke: var(--text-muted); stroke-width: 1.5; stroke-dasharray: 5 3; }
            .a-label { font: 700 14px var(--font-sans, system-ui); fill: var(--text-heading); text-anchor: middle; }
            .a-sub { font: 500 11px var(--font-sans, system-ui); fill: var(--text-muted); text-anchor: middle; }
            .a-badge { font: 700 10px var(--font-sans, system-ui); fill: var(--brand-primary); text-anchor: middle; letter-spacing: 0.12em; }
            .a-flow { fill: none; stroke: var(--brand-primary); stroke-width: 2; }
            .a-flow-soft { fill: none; stroke: var(--text-muted); stroke-width: 1.5; opacity: 0.7; }
            .a-annotation { font: italic 10.5px var(--font-sans, system-ui); fill: var(--text-muted); text-anchor: middle; }
            .a-arrow-head { fill: var(--brand-primary); }
          </style>

          <rect class="a-perimeter" x="30" y="60" width="840" height="320" rx="14" />
          <text class="a-perimeter-label" x="46" y="48">CUSTOMER PERIMETER</text>

          <rect class="a-box--soft" x="60" y="100" width="160" height="80" rx="8" />
          <text class="a-badge" x="140" y="122">IDENTITY</text>
          <text class="a-label" x="140" y="146">Your IdP</text>
          <text class="a-sub" x="140" y="164">SAML / OIDC / JWT</text>

          <path class="a-flow" d="M225 140 L295 140" marker-end="url(#a-arrow)" />
          <text class="a-annotation" x="260" y="130">identity</text>

          <rect class="a-box" x="300" y="80" width="220" height="120" rx="10" />
          <text class="a-badge" x="410" y="105">BOLT</text>
          <text class="a-label" x="410" y="130">Agent runtime</text>
          <text class="a-sub" x="410" y="152">orchestration · prompt assembly</text>
          <text class="a-sub" x="410" y="170">PII masking · audit chain</text>
          <text class="a-sub" x="410" y="188">multi-layer security pipeline</text>

          <path class="a-flow" d="M525 140 L595 140" marker-end="url(#a-arrow)" marker-start="url(#a-arrow)" />
          <text class="a-annotation" x="560" y="130">scoped retrieval</text>

          <rect class="a-box" x="600" y="80" width="240" height="120" rx="10" />
          <text class="a-badge" x="720" y="105">AEIRA</text>
          <text class="a-label" x="720" y="130">Data plane</text>
          <text class="a-sub" x="720" y="152">ACL pre-filter · identity-bound</text>
          <text class="a-sub" x="720" y="170">keyword + vector + graph index</text>
          <text class="a-sub" x="720" y="188">audit-trail receipts</text>

          <path class="a-flow-soft" d="M410 205 L410 240" marker-end="url(#a-arrow)" />

          <rect class="a-box--soft" x="60" y="245" width="780" height="100" rx="8" />
          <text class="a-badge" x="450" y="267">MCP-CONNECTED TOOLS &amp; SOURCES &middot; INSIDE YOUR PERIMETER</text>
          <text class="a-sub" x="450" y="293">M365 &middot; Google Workspace &middot; Slack &middot; Teams &middot; Jira &middot; Salesforce &middot; HubSpot &middot; ServiceNow</text>
          <text class="a-sub" x="450" y="311">Confluence &middot; Notion &middot; SharePoint &middot; GitHub &middot; GitLab &middot; Snowflake &middot; BigQuery &middot; 103 in-house connectors (plus ~350 via Airbyte)</text>
          <text class="a-sub" x="450" y="332">customer-owned credentials &middot; OAuth tokens encrypted in your Postgres</text>

          <path class="a-flow" d="M520 140 L820 140 L820 405" marker-end="url(#a-arrow)" />
          <text class="a-annotation" x="745" y="400">outbound HTTP &middot; PII-masked &middot; OpenAI-compatible</text>

          <rect class="a-box--ext" x="640" y="410" width="220" height="55" rx="8" />
          <text class="a-badge" x="750" y="430">LLM ENDPOINT</text>
          <text class="a-sub" x="750" y="452">Mode A / B / C &middot; you choose</text>
        </svg>
      </div>
    </PageSection>

    <PageSection
      eyebrow="Bolt — Enterprise Agent Platform"
      title="An agent runtime built for durability and trust."
      stripe="b"
    >
      <CardGrid cols={2}>
        <Card title="Inside your perimeter">
          Self-hosted on AWS, Azure, GCP, on-prem, or fully air-gapped. Identity flows through
          your existing IdP (SAML, OIDC, JWT). Your data, prompts, and responses stay inside your
          network. If you opt into Managed AI, only LLM calls route through a Sparcle-managed
          zero-retention gateway under an MSA + DPA; your data plane never leaves your perimeter.
        </Card>
        <Card title="Multi-layer security pipeline">
          Every prompt and response passes through a multi-layer pipeline covering identity
          validation, PII detection, policy guardrails, rate limiting, caching, audit logging,
          secrets management, and privacy-preserving context handling. The pipeline is
          patent-pending; specific layer implementations are reviewed under NDA.
        </Card>
        <Card title="Bring your own models">
          Your LLM (OpenAI, Anthropic, Bedrock, Vertex, Ollama, NVIDIA NIM, or your own
          fine-tune) — and your own embedding and reranking models too — all hot-swappable by
          config, and local in air-gap. With your own keys (BYOK) there's zero token markup.
          And keyword search plus the ACL pre-filter run with no model at all, so the base
          deploys CPU-only and fully offline; you add the semantic and LLM lanes only where you
          want them.
        </Card>
        <Card title="Durable agent lifecycle">
          Long-running agents survive process restarts, suspend on webhooks or timers, resume
          cleanly when results arrive. Sub-agent isolation, cancel cascades, heartbeat-leased
          multi-pod orchestration. Built for the work that doesn't finish in 5 seconds.
        </Card>
      </CardGrid>

      <div class="arch-diagram" style="margin-top: 2.5rem;" data-expandable tabindex="0" role="button" aria-label="Expand security pipeline diagram">
        <svg viewBox="0 0 560 720" class="arch-pipeline-svg" role="img" aria-labelledby="arch-pipeline-title arch-pipeline-desc">
          <title id="arch-pipeline-title">Bolt multi-layer security pipeline</title>
          <desc id="arch-pipeline-desc">Every prompt and response passes through eight ordered layers before the LLM call dispatches: identity validation, PII detection, policy guardrails, rate limiting, cache, audit logging, secrets management, and privacy-preserving context assembly. The pipeline is patent-pending; specific layer implementations are reviewed under NDA.</desc>

          <defs>
            <marker id="p-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="5" markerHeight="5" orient="auto-start-reverse">
              <path class="p-arrow-head" d="M0,0 L10,5 L0,10 z" />
            </marker>
          </defs>

          <style>
            .p-endpoint { fill: var(--brand-primary-light); stroke: var(--brand-primary); stroke-width: 1.5; }
            .p-endpoint-label { font: 700 12px var(--font-sans, system-ui); fill: var(--brand-primary); text-anchor: middle; letter-spacing: 0.06em; }
            .p-layer { fill: var(--bg-surface-solid); stroke: var(--brand-primary); stroke-width: 1.5; opacity: 0.95; }
            .p-layer-num { font: 700 11px var(--font-sans, system-ui); fill: var(--brand-primary); letter-spacing: 0.08em; }
            .p-layer-title { font: 700 13px var(--font-sans, system-ui); fill: var(--text-heading); }
            .p-layer-sub { font: 500 10.5px var(--font-sans, system-ui); fill: var(--text-muted); }
            .p-flow { fill: none; stroke: var(--brand-primary); stroke-width: 1.5; opacity: 0.6; }
            .p-arrow-head { fill: var(--brand-primary); opacity: 0.85; }
          </style>

          <rect class="p-endpoint" x="160" y="20" width="240" height="46" rx="6" />
          <text class="p-endpoint-label" x="280" y="48">INBOUND PROMPT</text>
          <path class="p-flow" d="M280 68 L280 82" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="86" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="108">01</text>
          <text class="p-layer-title" x="106" y="110">Identity validation</text>
          <text class="p-layer-sub" x="106" y="130">caller identity verified against IdP · session, scope, role</text>
          <path class="p-flow" d="M280 148 L280 162" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="166" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="188">02</text>
          <text class="p-layer-title" x="106" y="190">PII detection</text>
          <text class="p-layer-sub" x="106" y="210">private values scanned · tokenized before the LLM boundary</text>
          <path class="p-flow" d="M280 228 L280 242" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="246" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="268">03</text>
          <text class="p-layer-title" x="106" y="270">Policy guardrails</text>
          <text class="p-layer-sub" x="106" y="290">org-defined rules · model-tier floor · refusal hooks</text>
          <path class="p-flow" d="M280 308 L280 322" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="326" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="348">04</text>
          <text class="p-layer-title" x="106" y="350">Rate limiting</text>
          <text class="p-layer-sub" x="106" y="370">per-tenant, per-user budgets · abuse and runaway protection</text>
          <path class="p-flow" d="M280 388 L280 402" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="406" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="428">05</text>
          <text class="p-layer-title" x="106" y="430">Cache</text>
          <text class="p-layer-sub" x="106" y="450">semantic + exact-key tiers · LLM-cost reduction</text>
          <path class="p-flow" d="M280 468 L280 482" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="486" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="508">06</text>
          <text class="p-layer-title" x="106" y="510">Audit logging</text>
          <text class="p-layer-sub" x="106" y="530">event metadata sealed in the BoltAuditSink Merkle chain</text>
          <path class="p-flow" d="M280 548 L280 562" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="566" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="588">07</text>
          <text class="p-layer-title" x="106" y="590">Secrets management</text>
          <text class="p-layer-sub" x="106" y="610">customer-held credentials, KMS-wrapped at rest</text>
          <path class="p-flow" d="M280 628 L280 642" marker-end="url(#p-arrow)" />

          <rect class="p-layer" x="60" y="646" width="440" height="60" rx="6" />
          <text class="p-layer-num" x="76" y="668">08</text>
          <text class="p-layer-title" x="106" y="670">Privacy-preserving context</text>
          <text class="p-layer-sub" x="106" y="690">scoped assembly · only entitled, masked fragments dispatched</text>
        </svg>
      </div>
    </PageSection>

    <PageSection
      eyebrow="Aeira — Compliance-Grade Data Plane"
      title="The data plane your auditor signs off on."
      stripe="a"
    >
      <CardGrid cols={2}>
        <Card title="Identity-bound access">
          Every query is automatically filtered to what the calling user is entitled to see —
          region, department, sensitivity clearance, role — derived from your existing IdP.
          Your AI agents and your auditors see the same answer. No bypass mode for callers.
        </Card>
        <Card title="Provable erasure">
          When a tenant invokes the Right to be Forgotten under GDPR, or you need HIPAA-aligned
          erasure of restricted records, Aeira gives you cryptographic proof — not just a deleted
          row. Auditor-acceptable, independently verifiable. The specific KMS posture and
          key-management semantics are shared under NDA.
        </Card>
        <Card title="Audit-trail responses">
          Every result carries the why-filtered context regulators ask for during reviews. What
          was shown, what was suppressed, why. The artifact a compliance team can actually point
          to when asked "how did you ensure this AI system honored access controls?"
        </Card>
        <Card title="Air-gap-ready by default">
          Deploy in your VPC, on-prem, or fully air-gapped. All indexing, search, and retrieval
          run inside your security perimeter — no outbound calls, no telemetry, no third-party
          model API calls. Federated tier supports multi-region with offline-validated licensing
          for environments with no inbound or outbound connectivity.
        </Card>
      </CardGrid>
    </PageSection>

    <PageSection
      eyebrow="Authority Policy SDK — Customer-Extensible Layer"
      title="Your search. Your policy. Your code."
      lede="Every regulated vertical has its own honest definition of authoritative. The Authority Policy trait lets customers write theirs — in Rust, or in YAML config that wraps the reference packs — without forking the product. ACL handles authorization. Authority Policy handles ranking. Separate concerns, separate trait surfaces. Three reference packs ship in the box; vertical and customer-specific packs ship on engagement."
      stripe="b"
    >
      <CardGrid cols={2}>
        <Card title="Default pack">
          Universal signals every install benefits from — freshness, authority tier, approval
          lifecycle, audience targeting. Sensible defaults for any organization, regardless of
          vertical. Customers tune weights or swap in a vertical pack as needs evolve.
        </Card>
        <Card title="Regulated pack">
          Regulated-industry baseline — tighter freshness cadence, hard penalty on deprecated
          and archived records, emphasis on authority-tier sourcing. Compliance teams cite the
          right document, not the most recent draft of the wrong one.
        </Card>
        <Card title="Healthcare pack">
          Clinical-evidence ladder layered on the Regulated pack — clinical guidelines outrank
          clinical notes, expired-CME demotion, configurable per-customer evidence taxonomies.
          A starting point for customer-specific clinical ontology work.
        </Card>
        <Card title="Customer-written">
          Vertical packs and customer-specific impls slot into the same trait surface. Defense,
          finance, legal, federal-civilian engagements ship custom packs on the same SDK. Code
          lives in your repos, runs inside your perimeter, signed and reviewed by your team.
        </Card>
      </CardGrid>
      <div class="arch-diagram" style="margin-top: 2.5rem;" data-expandable tabindex="0" role="button" aria-label="Expand Authority Policy L4.5 placement diagram">
        <svg viewBox="0 0 720 600" class="arch-l45-svg" role="img" aria-labelledby="arch-l45-title arch-l45-desc">
          <title id="arch-l45-title">Authority Policy SDK — L4.5 placement</title>
          <desc id="arch-l45-desc">The retrieval pipeline as a vertical stack. Source systems feed Aeira indices. The ACL pre-filter drops rows the caller cannot see. Retrieval produces a candidate set. The Authority Policy SDK sits at L4.5, between retrieval and the final rank merge — this is the customer-extensible layer. RRF rank merge then produces the final ordering, and the result plus its receipt are returned to the agent or LLM.</desc>

          <defs>
            <marker id="l-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
              <path class="l-arrow-head" d="M0,0 L10,5 L0,10 z" />
            </marker>
          </defs>

          <style>
            .l-band { fill: var(--bg-surface-solid); stroke: var(--text-muted); stroke-width: 1; opacity: 0.92; }
            .l-band--highlight { fill: var(--brand-primary-light); stroke: var(--brand-primary); stroke-width: 2; opacity: 1; }
            .l-band-num { font: 700 11px var(--font-sans, system-ui); fill: var(--text-muted); letter-spacing: 0.12em; }
            .l-band-num--highlight { font: 700 11px var(--font-sans, system-ui); fill: var(--brand-primary); letter-spacing: 0.12em; }
            .l-band-title { font: 700 14px var(--font-sans, system-ui); fill: var(--text-heading); }
            .l-band-sub { font: 500 11.5px var(--font-sans, system-ui); fill: var(--text-muted); }
            .l-flow { fill: none; stroke: var(--text-muted); stroke-width: 1.5; opacity: 0.55; }
            .l-arrow-head { fill: var(--text-muted); opacity: 0.7; }
            .l-callout { font: italic 11px var(--font-sans, system-ui); fill: var(--brand-primary); }
            .l-callout-title { font: 700 11px var(--font-sans, system-ui); fill: var(--brand-primary); letter-spacing: 0.06em; }
          </style>

          <rect class="l-band" x="100" y="20" width="520" height="60" rx="8" />
          <text class="l-band-num" x="116" y="42">L1</text>
          <text class="l-band-title" x="148" y="44">Source systems</text>
          <text class="l-band-sub" x="148" y="64">DMS · CRM · M365 · Slack · Jira · Drive · 103 in-house connectors (plus ~350 via Airbyte)</text>

          <path class="l-flow" d="M360 82 L360 100" marker-end="url(#l-arrow)" />

          <rect class="l-band" x="100" y="105" width="520" height="60" rx="8" />
          <text class="l-band-num" x="116" y="127">L2</text>
          <text class="l-band-title" x="148" y="129">Aeira indices</text>
          <text class="l-band-sub" x="148" y="149">keyword + vector + graph · sharded inside your perimeter</text>

          <path class="l-flow" d="M360 167 L360 185" marker-end="url(#l-arrow)" />

          <rect class="l-band" x="100" y="190" width="520" height="60" rx="8" />
          <text class="l-band-num" x="116" y="212">L3</text>
          <text class="l-band-title" x="148" y="214">ACL pre-filter</text>
          <text class="l-band-sub" x="148" y="234">identity-bound · drops rows the caller is not entitled to see</text>

          <path class="l-flow" d="M360 252 L360 270" marker-end="url(#l-arrow)" />

          <rect class="l-band" x="100" y="275" width="520" height="60" rx="8" />
          <text class="l-band-num" x="116" y="297">L4</text>
          <text class="l-band-title" x="148" y="299">Retrieval</text>
          <text class="l-band-sub" x="148" y="319">candidate set · only entitled rows enter the ranker</text>

          <path class="l-flow" d="M360 337 L360 355" marker-end="url(#l-arrow)" />

          <rect class="l-band--highlight" x="100" y="360" width="520" height="80" rx="8" />
          <text class="l-band-num--highlight" x="116" y="383">L4.5</text>
          <text class="l-band-title" x="148" y="385">Authority Policy SDK</text>
          <text class="l-band-sub" x="148" y="405" style="fill: var(--text-body)">customer-extensible ranking · freshness, approval, evidence ladder</text>
          <text class="l-band-sub" x="148" y="423" style="fill: var(--text-body)">three reference packs ship in the box · write your own in Rust or YAML</text>

          <text class="l-callout-title" x="606" y="385" text-anchor="end">★ EXTEND HERE</text>

          <path class="l-flow" d="M360 442 L360 460" marker-end="url(#l-arrow)" />

          <rect class="l-band" x="100" y="465" width="520" height="60" rx="8" />
          <text class="l-band-num" x="116" y="487">L5</text>
          <text class="l-band-title" x="148" y="489">RRF rank merge</text>
          <text class="l-band-sub" x="148" y="509">reciprocal rank fusion · keyword + vector + Authority Policy</text>

          <path class="l-flow" d="M360 527 L360 545" marker-end="url(#l-arrow)" />

          <rect class="l-band" x="100" y="550" width="520" height="40" rx="8" />
          <text class="l-band-num" x="116" y="572">L6</text>
          <text class="l-band-title" x="148" y="574">Result + receipt</text>
          <text class="l-band-sub" x="350" y="574">to agent / LLM</text>
        </svg>
      </div>

      <p class="page-section__lede" style="margin-top: 2rem; font-size: 1rem;">
        The trait surface and reference impls live in <code>aeira-authority-policy</code>.
        Full-detail poster:
        <a href="/docs/pdfs/bolt-aeira-architecture-poster.pdf">bolt-aeira-architecture-poster.pdf</a>.
      </p>
    </PageSection>

    <PageSection
      eyebrow="The full architectural view"
      title="Every component that ships, in one diagram."
      lede="The print-quality architecture poster, embedded below. Bolt's client surfaces on top; Aeira's three internal layers (search pipeline, storage and cryptography, ingest spine and cache); the connector catalog grouped by category; the three external integrations (your IdP, your LLM, your KMS) outside the perimeter. The Authority Policy SDK is the customer-extensible wedge — highlighted in the search pipeline."
      stripe="a"
    >
      <p class="arch-pdf-fallback-link">
        If the embed below doesn't render in your browser, <a href="/docs/pdfs/bolt-aeira-architecture-poster.pdf" target="_blank" rel="noopener">open the poster PDF in a new tab</a> or use the download button at the bottom of this section.
      </p>

      <div class="arch-pdf-embed" data-expandable tabindex="0" role="button" aria-label="Expand architecture poster">
        <iframe
          src="/docs/pdfs/bolt-aeira-architecture-poster.pdf#view=FitH"
          title="Bolt + Aeira architecture poster"
          loading="lazy"
        ></iframe>
        <noscript>
          <p class="arch-pdf-fallback">
            Your browser blocked the embed. <a href="/docs/pdfs/bolt-aeira-architecture-poster.pdf">Open the poster PDF in a new tab.</a>
          </p>
        </noscript>
      </div>

      <p class="arch-pdf-actions">
        <a href="/docs/pdfs/bolt-aeira-architecture-poster.pdf" class="btn btn-secondary btn-lg" download>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
            <polyline points="7 10 12 15 17 10" />
            <line x1="12" y1="15" x2="12" y2="3" />
          </svg>
          Download poster PDF
        </a>
        <a href="/docs/pdfs/bolt-aeira-architecture-poster.pdf" target="_blank" rel="noopener" class="btn btn-tertiary btn-lg">
          Open in new tab &nearr;
        </a>
      </p>
    </PageSection>


    <PageSection
      eyebrow="Tech stack"
      title="What ships under the hood."
      lede="The runtime is Rust end-to-end. The search substrate is pluggable — keyword on Postgres FTS or Quickwit, vector on pgvector or Qdrant — selected by config, not code. The audit chain is Ed25519-signed Merkle epochs. The full list below — every layer is swappable; reference impls ship in the box."
      stripe="b"
    >
      <div class="tech-stack-grid">
        <div class="tech-stack-col">
          <h3 class="tech-stack-col__title">Runtime &amp; API</h3>
          <dl class="tech-stack-list">
            <div><dt>Language</dt><dd>Rust 1.85+</dd></div>
            <div><dt>Workspace</dt><dd>~130 crates</dd></div>
            <div><dt>HTTP layer</dt><dd>axum &middot; tower</dd></div>
            <div><dt>Async runtime</dt><dd>tokio</dd></div>
            <div><dt>Auth</dt><dd>OIDC &middot; SAML &middot; JWT</dd></div>
            <div><dt>Telemetry</dt><dd>OpenTelemetry &middot; Prometheus</dd></div>
          </dl>
        </div>

        <div class="tech-stack-col">
          <h3 class="tech-stack-col__title">Search &amp; ranking</h3>
          <dl class="tech-stack-list">
            <div><dt>Keyword</dt><dd>Postgres FTS &middot; Quickwit</dd></div>
            <div><dt>Vector</dt><dd>pgvector &middot; Qdrant</dd></div>
            <div><dt>Fusion</dt><dd>RRF (weighted)</dd></div>
            <div><dt>Policy</dt><dd>Authority Policy SDK</dd></div>
            <div><dt>ReBAC</dt><dd>OpenFGA <em>pilot</em></dd></div>
            <div><dt>Cache L1</dt><dd>LRU</dd></div>
            <div><dt>Cache L2</dt><dd>Redis <em>pilot</em></dd></div>
          </dl>
        </div>

        <div class="tech-stack-col">
          <h3 class="tech-stack-col__title">Ingest &amp; storage</h3>
          <dl class="tech-stack-list">
            <div><dt>Spine</dt><dd>NATS JetStream</dd></div>
            <div><dt>Storage</dt><dd>FS + S3 Object Lock <em>pilot</em></dd></div>
            <div><dt>KMS</dt><dd>Vault &middot; AWS KMS &middot; PKCS#11</dd></div>
            <div><dt>Audit</dt><dd>Ed25519 + Merkle</dd></div>
          </dl>
        </div>

        <div class="tech-stack-col">
          <h3 class="tech-stack-col__title">Clients</h3>
          <dl class="tech-stack-list">
            <div><dt>Desktop</dt><dd>Native &middot; macOS &middot; Linux &middot; Windows</dd></div>
            <div><dt>Web</dt><dd>Svelte 5 &middot; installable PWA</dd></div>
            <div><dt>Browser ext</dt><dd>Chrome Manifest v3</dd></div>
            <div><dt>Mobile</dt><dd>iOS &middot; Android <em>Q3 2026+</em></dd></div>
          </dl>
        </div>
      </div>
    </PageSection>

    <PageSection
      eyebrow="What ships today vs in pilot"
      title="Honest framing on shipped vs in-flight."
      lede="The same posture the trust center uses, repeated here so you can scope a pilot against shipped functionality and know what activates with your design-partner engagement."
      stripe="a"
    >
      <div class="shipped-pilot-grid">
        <div class="shipped-card shipped-card--shipped">
          <div class="shipped-card__header">
            <span class="shipped-card__icon" aria-hidden="true">&check;</span>
            <h3>Shipped today</h3>
          </div>
          <ul>
            <li>103 in-house source connectors + Airbyte bridge (~350 sources) plus any MCP-compliant server</li>
            <li>4-axis ACL pre-filter at index time (provable in the audit chain)</li>
            <li>Authority Policy SDK + 3 reference impls (Default · Regulated · Healthcare)</li>
            <li>Sealed Merkle audit chain + standalone verifier binary</li>
            <li>3 deployment topologies: laptop sidecar · docker-compose · Helm</li>
            <li>4 KMS variants: Vault · AWS KMS · PKCS#11 HSM · local-dev</li>
            <li>Cross-product audit byte-compat between Bolt and Aeira</li>
          </ul>
        </div>

        <div class="shipped-card shipped-card--pilot">
          <div class="shipped-card__header">
            <span class="shipped-card__icon" aria-hidden="true">&#9681;</span>
            <h3>Ships during pilot</h3>
          </div>
          <ul>
            <li>End-to-end ingest saga (1–2 days from green-light)</li>
            <li>Real-time webhook receivers for top-10 sources</li>
            <li>L2 Redis permission cache + webhook invalidation</li>
            <li>OpenFGA wire-up for tuple-mode ACL</li>
            <li>Late-bind on next 14 connectors (per design-partner priority)</li>
            <li>First published latency benchmark (target p95 ≤ 200ms on 100M-doc tenant)</li>
          </ul>
        </div>
      </div>
    </PageSection>

    <PageSection
      eyebrow="Deployment"
      title="Two operating models. Same platform."
      lede="Bolt and Aeira always self-host the data plane and hosting in your own perimeter — Sparcle never hosts your data. The only choice is who runs the AI inference: bring your own LLM, or opt into a Sparcle-managed zero-retention gateway."
      stripe="b"
    >
      <div class="data-table data-table--cols3">
        <div class="data-table__row data-table__row--head">
          <div></div>
          <div>Self-Hosted</div>
          <div>Managed AI option</div>
        </div>
        {tierRows.map((row) => (
          <div class="data-table__row">
            <div class="data-table__label">{row[0]}</div>
            <div>{row[1]}</div>
            <div>{row[2]}</div>
          </div>
        ))}
      </div>
    </PageSection>

    <PageSection
      eyebrow="Intellectual Property"
      title="Patent-pending architecture, verifiable today."
      lede="Bolt's patent-pending architecture covers the runtime architecture, the priority engine, and the overlay UI. Detailed claim descriptions, novelty analysis, and implementation specifics are shared during architecture reviews under NDA."
      stripe="a"
      width="prose"
    >
      <span aria-hidden="true"></span>
    </PageSection>

    <CTAStrip
      variant="band"
      title="Ready to look at the details?"
      lede="Architecture briefs, security posture documentation, deployment guides, and patent claim summaries are shared during pilot evaluation under NDA. Schedule a 30-minute call and we'll cover what's relevant to your environment."
    >
      <a href="#" onclick="openContactModal('Architecture Review'); return false;" class="btn btn-primary btn-lg">
        Schedule Architecture Review
      </a>
      <a href="/security" class="btn btn-secondary btn-lg">
        Security posture &rarr;
      </a>
    </CTAStrip>
  </main>
</BaseLayout>

<style is:global>
  /* 3-column data-table variant — for the operating-model matrix.
     Shares the .data-table base styles defined globally on /security. */
  .data-table--cols3 .data-table__row {
    grid-template-columns: 180px repeat(2, 1fr);
  }
  @media (max-width: 720px) {
    .data-table--cols3 .data-table__row {
      grid-template-columns: 1fr;
    }
  }

  /* Architecture diagrams — shared container + scaling rules. The SVGs
     themselves carry their own embedded <style> that wires to CSS vars,
     so they theme-track in both light and dark mode. */
  .page-architecture .arch-diagram {
    background: var(--bg-surface-solid);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-lg);
    padding: 1.5rem 1.75rem;
    max-width: 940px;
    margin: 0 auto;
  }
  .page-architecture .arch-overview-svg,
  .page-architecture .arch-l45-svg,
  .page-architecture .arch-pipeline-svg {
    display: block;
    width: 100%;
    height: auto;
    margin: 0 auto;
  }
  .page-architecture .arch-overview-svg { max-width: 900px; }
  .page-architecture .arch-l45-svg { max-width: 880px; }
  .page-architecture .arch-pipeline-svg { max-width: 560px; }

  /* The full-detail architecture diagram is intentionally information-dense
     — text size requires roughly 1100px of render width to stay readable.
     Below that, we let the container scroll horizontally rather than
     compressing the SVG into an unreadable thumbnail. */
  .page-architecture .arch-diagram--wide {
    max-width: 1180px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }
  .page-architecture .arch-full-svg {
    display: block;
    width: 100%;
    min-width: 1100px;
    height: auto;
    margin: 0 auto;
  }

  /* PDF poster embed — iframe shows the print-quality artifact inline.
     Clicking the wrapper opens the same iframe full-screen via the
     global diagram-modal. */
  .page-architecture .arch-pdf-embed {
    background: var(--bg-surface-solid);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-lg);
    padding: 0.5rem;
    max-width: 1180px;
    margin: 0 auto;
    overflow: hidden;
  }
  .page-architecture .arch-pdf-embed iframe {
    display: block;
    width: 100%;
    height: 760px;
    border: 0;
    border-radius: var(--radius-md);
    background: #f5f7fa;
  }
  .page-architecture .arch-pdf-fallback {
    padding: 1rem 1.25rem;
    color: var(--text-muted);
    text-align: center;
    margin: 0;
  }
  /* Fallback link above the embed — sits between the lede and the iframe
     so users with PDF-rendering disabled (Brave shields, locked-down
     enterprise browsers) have a path before they even try the embed. */
  .page-architecture .arch-pdf-fallback-link {
    margin: 0 auto 1.25rem;
    max-width: 1180px;
    font-size: 0.9rem;
    color: var(--text-muted);
    text-align: center;
    padding: 0.85rem 1.1rem;
    background: var(--brand-primary-light);
    border-radius: var(--radius-md);
    border: 1px solid var(--brand-primary);
  }
  .page-architecture .arch-pdf-fallback-link a {
    color: var(--brand-primary);
    font-weight: 600;
    text-decoration: underline;
  }
  .page-architecture .arch-pdf-fallback-link a:hover {
    color: var(--brand-primary-hover);
  }
  .page-architecture .arch-pdf-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    margin: 1.5rem 0 0;
  }

  /* Tech stack — 4-column dense table-like grid of name/value pairs.
     Matches the right-side column on the PDF poster. */
  .page-architecture .tech-stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
  }
  .page-architecture .tech-stack-col {
    background: var(--bg-surface-solid);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-lg);
    padding: 1.5rem 1.4rem;
  }
  .page-architecture .tech-stack-col__title {
    margin: 0 0 1rem;
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--brand-primary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding-bottom: 0.6rem;
    border-bottom: 1px solid var(--border-subtle);
  }
  .page-architecture .tech-stack-list {
    margin: 0;
    display: grid;
    gap: 0.65rem;
  }
  .page-architecture .tech-stack-list > div {
    display: grid;
    grid-template-columns: 95px 1fr;
    gap: 0.6rem;
    align-items: baseline;
    font-size: 0.88rem;
  }
  .page-architecture .tech-stack-list dt {
    color: var(--text-muted);
    font-weight: 600;
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }
  .page-architecture .tech-stack-list dd {
    margin: 0;
    color: var(--text-body);
    line-height: 1.5;
  }
  .page-architecture .tech-stack-list em {
    font-style: normal;
    color: var(--brand-primary);
    font-size: 0.7rem;
    font-weight: 700;
    background: var(--brand-primary-light);
    padding: 0.15rem 0.45rem;
    border-radius: var(--radius-full);
    margin-left: 0.3rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  /* Shipped vs Pilot — two side-by-side card columns matching the
     PDF poster footer. */
  .page-architecture .shipped-pilot-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
    gap: 1.5rem;
  }
  .page-architecture .shipped-card {
    background: var(--bg-surface-solid);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-lg);
    padding: 1.75rem 1.6rem;
  }
  .page-architecture .shipped-card--shipped {
    border-left: 4px solid var(--color-success-strong, var(--brand-primary));
  }
  .page-architecture .shipped-card--pilot {
    border-left: 4px solid var(--brand-primary);
  }
  .page-architecture .shipped-card__header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
    padding-bottom: 0.85rem;
    border-bottom: 1px solid var(--border-subtle);
  }
  .page-architecture .shipped-card__header h3 {
    margin: 0;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--text-heading);
    font-family: var(--font-display);
  }
  .page-architecture .shipped-card__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    font-size: 1.1rem;
    font-weight: 700;
  }
  .page-architecture .shipped-card--shipped .shipped-card__icon {
    background: var(--color-success-bg);
    color: var(--color-success-strong);
  }
  .page-architecture .shipped-card--pilot .shipped-card__icon {
    background: var(--brand-primary-light);
    color: var(--brand-primary);
  }
  .page-architecture .shipped-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.7rem;
  }
  .page-architecture .shipped-card li {
    position: relative;
    padding-left: 1.4rem;
    font-size: 0.94rem;
    line-height: 1.55;
    color: var(--text-body);
  }
  .page-architecture .shipped-card--shipped li::before {
    content: "\2713";
    position: absolute;
    left: 0;
    top: 0.05rem;
    color: var(--color-success-strong);
    font-weight: 700;
  }
  .page-architecture .shipped-card--pilot li::before {
    content: "\25D1";
    position: absolute;
    left: 0;
    top: 0.05rem;
    color: var(--brand-primary);
    font-weight: 700;
  }
</style>
